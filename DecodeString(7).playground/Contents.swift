import UIKit
/*
Given an encoded string, return its decoded version.
The encoding rule is: k[encoded_string], where the encoded string inside the square brackets is repeated exactly k times.

You can assume the input is always valid, and the digits only represent repeat counts.
 
 examples
 decodeString("3[a]2[bc]")        // "aaabcbc"
 decodeString("3[a2[c]]")         // "accaccacc"
 decodeString("2[abc]3[cd]ef")    // "abcabccdcdcdef"
 decodeString("10[a]")            // "aaaaaaaaaa"
*/
var greeting = "Hello, playground"

func decodeString(_ string: String) {
    // map the string into a dictionary
    string.map { character in
        
    }
}
